<% if notice %>
    <p id="notice"><%= notice %></p>
<% end %>
<table  id="films" class="display">
  <thead>
  <tr>
    <th>ID</th>
    <th>Titel</th>
    <th>Kategorie</th>
    <th>Länge</th>
    <th>Land</th>
    <th>Jahr</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @films.each do |film| %>
      <tr>
        <td><%= film.id %></td>
        <td><%= film.titel %></td>
        <td><%= film.category.name %></td>
        <td><%= film.laenge %></td>
        <td><%= film.land %></td>
        <td><%= film.jahr %></td>
        <td><%= link_to (image_tag "view.png", :title =>'Show'), film %></td>
        <td><%= link_to (image_tag "edit.jpg", :title =>'Edit'), edit_film_path(film) %></td>
        <td><%= link_to (image_tag "delete.jpg", :title => 'Delete'), film, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<br>
<%= link_to 'New Film', new_film_path %>


