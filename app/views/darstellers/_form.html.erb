<br>
<%= form_for(@darsteller) do |f| %>
    <% if @darsteller.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@darsteller.errors.count, "error") %> prohibited this darsteller from being saved:</h2>

          <ul>
            <% @darsteller.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <br>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
