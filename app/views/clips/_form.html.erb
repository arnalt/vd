<%= form_for(@clip) do |f| %>
  <% if @clip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clip.errors.count, "error") %> prohibited this clip from being saved:</h2>

      <ul>
      <% @clip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :clipname %><br />
    <%= f.text_field :clipname %>
  </div>
  <div class="field">
    <%= f.label :actor %><br />
    <%= f.text_field :actor %>
  </div>
    <div class="field">
      <%= f.label :movie_type %><br />
      <%= f.text_field :movie_type %>
    </div>
  <div class="field">
    <%= f.label :clip_url %><br />
    <%= f.text_field :clip_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
